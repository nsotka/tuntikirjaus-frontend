import React, { Component } from 'react'

class HeaderTabText extends Component {
    render() {
        return (
            <p className="my-auto">{this.props.headerText}</p>
        )
    }
}

export default HeaderTabText
